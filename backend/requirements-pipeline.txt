# 파이프라인(전처리/STT/음성분석/슬라이드 추출) 단계에서만 필요.
# API 서버 띄울 때는 안 깔아도 됨.
# 무거운 패키지(PyTorch 포함)이므로 파이프라인 작성 시점에 설치.

# STT: openai-whisper 대신 faster-whisper.
#   - Python 3.13 에서 openai-whisper 빌드 실패(pkg_resources) 해소
#   - CTranslate2 기반이라 CPU 에서 수 배 빠르고 torch 불필요
#   - word_timestamps=True 네이티브 지원 (STEP 3 가 단어 경계를 요구)
#   - Silero VAD(ONNX) 번들 → 무음 검출도 추가 설치 없이 해결
faster-whisper==1.2.1

# librosa 는 STEP 3 에서 안 씀 (numba 의존 → Python 3.13 + numpy 2.x 호환 이슈).
# 음향 특징은 stt_test/audio.py 에서 순수 numpy 로 구현.
librosa==0.10.2.post1
opencv-python-headless>=4.10   # 서버는 GUI 불필요 → headless. STEP 2 스마트 청킹(motion 시계열)에 사용
pdfplumber==0.11.4

# STEP 2 VLM (Gemini)
google-genai==0.3.0
python-dotenv==1.0.1
